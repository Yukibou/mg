%tr.nested-fields
  = f.hidden_field :id
  %td
    = f.collection_select :gear_id, f.object.selectable_gears(current_user), :id, :display_name, {}, class: 'form-control'
  %td
    = link_to_remove_association '削除', f, class: 'btn btn-default'